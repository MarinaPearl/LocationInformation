package com.demchuk.locationinformation.locationinformation.VC;

import com.demchuk.locationinformation.locationinformation.Worker;

public class Model {
    public void callWorker() {
        Worker worker = new Worker();
        worker.work();
    }
}
